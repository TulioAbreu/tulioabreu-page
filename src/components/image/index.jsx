export const Image = ({ id, className, src, alt, width, height }) => (
    <img
        id={id}
        className={className}
        src={src}
        alt={alt}
        width={width}
        height={height}
    />
);
